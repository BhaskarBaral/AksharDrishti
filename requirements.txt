fastapi
uvicorn[standard]
python-multipart
python-dotenv
pillow
pytesseract
jiwer
requests
easyocr
paddleocr
paddlepaddle==3.2.2
# transformers/huggingface_hub/tokenizers pinned together: paddleocr's
# paddlenlp dependency pins tokenizers==0.19.1, and transformers>=4.43 needs
# tokenizers>=0.19.5. transformers 4.42.4 + huggingface_hub<1.0 is the last
# combination that satisfies both without patching paddlenlp -- newer
# transformers (5.x) requires huggingface_hub>=1.0, which removes
# is_offline_mode() and breaks the TrOCR engine's import at call time.
transformers==4.42.4
huggingface_hub<1.0,>=0.23
sentencepiece
numpy<2

# torch/torchvision (CUDA build) and paddlepaddle-gpu are installed separately
# by setup_gpu.ps1 -- their correct wheel depends on the resolved CUDA tag,
# which isn't expressible as a plain requirements.txt pin.
#
# paddlepaddle must be pinned to 3.2.2, not the latest 3.3.x: 3.3.0/3.3.1 have a
# CPU (oneDNN/PIR) regression that raises NotImplementedError on every predict()
# call -- https://github.com/PaddlePaddle/Paddle/issues/77340
